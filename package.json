{
  "name": "casamento-wdmar-adylla",
  "version": "1.0.0",
  "private": true,
  "description": "Site do casamento — funções serverless (Vercel) para pagamento via Stripe",
  "scripts": {
    "serve": "vercel dev --listen 3000",
    "dev": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
